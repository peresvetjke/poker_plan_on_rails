<% if round.errors.any? %>
  <div class="error-message">
    <%= round.errors.full_messages.to_sentence.capitalize %>
  </div>
<% end %>
<%= form_with model: round do |f| %>
  <%= f.text_field :title %>
  <%= link_to "Cancel", rounds_path %>
  <%= f.submit "Save" %>
<% end %>
