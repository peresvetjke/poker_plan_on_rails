<% flash.each do |flash_type, message| %>
  <article class="message is-link flash__message" data-controller="removals" data-action="animationend->removals#remove">
    <div class="message-body">
      <%= message %>
    </div>
  </article>

<% end %>
