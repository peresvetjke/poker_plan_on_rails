<%= turbo_stream.replace @task %>
<%= turbo_stream.update "round_#{@round.id}_user_#{current_user.id}_estimation_panel",
      target: "round_#{@round.id}_user_#{current_user.id}_estimation_panel" do %>
  <%= render EstimationPanel::Component.new(round: @round, user: current_user, value: @value) %>
<% end %>
