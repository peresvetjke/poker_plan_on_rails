<%= link_to sanitize("&larr; Back to rounds"), rounds_path %>
<div>
  <h1>
    <%= @round.title %>
  </h1>

  <ul>
    <% @round.users.each do |user| %>
      <li><%= user.username %></li>
    <% end %>
  </ul>

  <%= render @round.tasks %>
</div>
